=== Chrome Web Store – Privacy / Single purpose & permission justifications ===

--- Single purpose ---

This extension has one purpose: helping users remove favorite videos from their own TikTok account in bulk. It loads the first Favorites grid batch, applies the optional keyword filter, and asks for explicit confirmation. It then filters and removes one loaded batch at a time before scrolling for more, so large libraries do not require a complete pre-scan. A full pre-scan runs only when the user explicitly selects read-only analysis mode. It reloads and scans the grid again after removal to verify which favorites disappeared and which remain. Processing happens locally in the browser and no data is sent to the developer or any server other than TikTok.

--- scripting permission ---

The scripting permission runs the content script and in-page control panel on TikTok, reads the page session context needed to identify the signed-in account, and performs a user-confirmed favorite-removal request in TikTok's MAIN page world so it uses the existing TikTok session.

--- tabs permission ---

The tabs permission opens the user's TikTok profile, focuses an already active run, and communicates only with that TikTok tab. The extension does not inspect or modify unrelated tabs.

--- storage permission ---

The storage permission saves local popup settings and a temporary chrome.storage.session active-job marker. The marker prevents overlapping runs if Chrome suspends the Manifest V3 service worker and automatically expires after 12 hours. This data stays on the user's device.

--- host permission ---

The https://www.tiktok.com/* host permission is required because the extension reads the user's Favorites grid, performs confirmed removal requests through TikTok's web endpoint, and scans the grid again for verification. No other website host is accessed.

--- Data use ---

The extension does not collect, sell, transfer, or use personal data for advertising, analytics, credit, or any unrelated purpose. TikTok session information is used transiently on the device only to perform the requested operation. Reports are generated locally and downloaded only when the user chooses.
